# 核心规则：所有文本文件自动识别，提交时强制 LF
* text=auto eol=lf

# 二进制文件（不转换换行符，避免损坏）
*.png *.jpg *.jpeg *.gif *.ico binary
*.zip *.tar *.gz *.7z binary
*.woff *.woff2 *.ttf *.eot binary

# 明确指定前端/配置文件的换行符（兜底）
*.mts *.ts *.js *.vue *.md *.json *.yml *.yaml text eol=lf
*.html *.css *.scss *.less text eol=lf